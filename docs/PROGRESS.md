# サイスピ 進行記録

更新日: 2026-09-15 / 計画: v1.1 / P4演出・共有・ランキング公開準備

## 現在の状態

P0の計画と資料はPR #1、P1の土台はPR #3、P2の目標・計測と開始合図はPR #4・#5でmainへ反映済み。Pages Actions化はPR #6でmainへ反映済みで、P3もPR #7でmainへ反映済みである。今回の基準mainは `9632386d2887fb2a6297b26dff315e87d6d4ae5d`。今回のP4では、3秒の光柱、共有文言・OGP画像、リタイア、再プレイ、長押しコピー抑止、サイノメ準拠の描画品質とランキング登録確認を追加した。Supabase本番の`saisupi`ゲーム登録と実通信確認は完了し、既存の権限・RLS・RPC、iPhone実機確認は未変更・未実施である。

| 工程 | 状態 | 記録 |
|---|---|---|
| P0 計画と調査資料 | 反映済み | PR #1のマージをGitHubで確認した。 |
| P1 元ゲームの流用 | 反映済み | 元版固定コミットの盤面・カメラ・サイコロ・キャラクター・操作を新作へ移植し、埋まった1個へ登って上昇完了まで動く土台を作った。旧ルールは実行経路へ入れていない。PR #3のmainへのマージを確認した。 |
| P2 新ルールと計測 | 反映済み | 配置案Aを版付きの暫定設定として固定し、10目標の一括生成、上昇完了起点の時計、上面一致と重複防止、完走時の0.01秒表示をPR #4でmainへ反映した。完全露出時の一回発光はPR #5でmainへ反映した。 |
| P3 演出・結果・共有 | main反映済み | 一致マスから立ち上がる半透明光柱、サイノメと同じフリック・転がり・成功SE、結果詳細10件、再プレイ、ホーム・実験場・共有導線を追加した。PR #7でmainへの反映を確認した。 |
| P4 演出・ランキング公開準備 | 実装済み・Supabase登録・実通信確認済み | 3秒光柱、光る目標、正確な共有文、OGPサムネイル、効果音デフォルトオン、リタイア→結果、結果画面内再プレイ、長押しコピー抑止、ランキング登録確認を追加した。`public.games`へ`saisupi`を有効・短い時間が上位・0.01秒単位・共有受付として登録し、公開用キーで`record_game_play`、`submit_score`、`get_best_score_ranking`の実通信を確認した。既存の権限・RLS・RPCは変更していない。 |
| P5 実機・公開前検査 | 未着手 | iPhone Safariでの新作検査は未実施。 |

## P0で確認した事実（履歴）

保存先は初期コミット `6b254ab49dc887c98dd35f56fa03cb146533bb10` のREADMEだけだった。元のサイノメの最新コミットは `2103b8a91f214fc8698d119113417d5360f990f0` で、前の調査版と一致した。

元コードには埋まったサイコロへ登る処理と上昇処理がある。新作の開始条件はその完了イベントへ接続する計画とした。タッチ向けと標準の移動時間に差があるため、無断で統一せず記録の比較条件を別に決める。

クオンパの上面一致はPS1版のゲーム内説明画像を確認した。時計はW3CのHigh Resolution Time資料を確認した。外部の画像・マニュアル全文・ゲームデータはリポジトリへ転載せず、出典と自作の要約を保存した。

## P0で報告した計画用の検証（今回の再実行ではない）

独立した参照モデルで7×7の位置とサイコロ姿勢を探索した。指定した基本姿勢から到達できる状態は588、姿勢の種類は24。配置案Aの全10座標について上面1〜6へ到達でき、60条件を確認した。逆方向の往復と対面の合計7も確認した。

提案した0.01秒未満切捨てについて、8個の整数ミリ秒の境界例を確認した。再現方法と限界は[計測と検証の資料](references/TIMING_AND_VALIDATION.md)にある。

これらはゲームコードのテストではない。新作の描画・操作・保存・オンライン記録・iPhone実機を検査したことにはならない。

## 未確定と未確認

具体的な10座標、開始位置と姿勢、端数処理の採用、目標出現後の中断記録の扱い、異なる操作速度の公式比較方法は未確定または未確認である。サイスピのオンライン接続契約は、既存RPCを使った実通信まで確認した。詳細は[決定事項](DECISIONS.md)に記録した。

以前の会話にあったランダム盤面100件の最短手数の数値は、その再現用の問題一覧と実行物を今回確認していない。さらに今回は位置が固定に変更されたため、その数値を本作の難易度基準には使わない。

## P2で追加した確認

- 目標座標の盤面内・整数・重複なし・開始サイコロとの非重複を検査する。
- 乱数は登り完了後に10回だけ読み、1〜6へ変換し、達成や再描画で引き直さない。
- 時計は注入可能な単調時刻を使い、完全露出時に開始、10個目の移動完了後の一致時に終了する。
- `RUNNING`中の同一目標の再訪を1回として扱い、`FINISHED`後の入力・終了を無視する。

## P3で追加した確認

- 一致した目標だけで光柱と成功SEが1回発火し、光柱は3,000ms後に破棄される。
- 目標ごとに完全露出からの経過時間を保持し、結果画面で1〜10を固定順に表示する。
- ホーム・結果の共有本文には、URLをラベルや埋め込みにせず、独立した行のテキストとして含める。
- プレイ開始ごとに`record_game_play`を1回、完走ごとに`submit_score`を1回、結果画面で上位10件を取得する。
- 方向キー/WASDはサイノメの対応を維持し、非入力要素の`contextmenu`・選択・copy・cut・paste・dragstartを抑止する。

## P4後のSupabase確認

- `public.games`に`saisupi`を追加し、`is_active=true`、`score_order=asc`、`score_unit=秒`、`score_scale=100`、`score_decimals=2`、`submission_mode=shared`を確認した。
- 公開用キーでゲーム登録の読み取り、プレイ開始、`123.45秒`のスコア登録、上位10件取得が成功した。検証用の記録は確認後に削除し、ランキングには残していない。
- `public.games`の有効行ポリシーと、既存3RPCの匿名実行権限を確認した。既存のセキュリティ助言に対する修正は今回の範囲外である。

## 次の作業

P4の独立レビュー、GitHub Actionsの実行確認、iPhone Safariを含む実機検査を行う。配置案・丸め方・中断記録・異なる操作速度の公式比較方法は、決定事項に従い未確定として扱う。

## AI指示の整理

入口、通常実装の許可、独立レビュー用スキル、文書の参照関係を整理した。理由・今回の検証・未実施範囲は[整理記録](ai/INSTRUCTION_AUDIT.md)を参照する。この変更はゲーム仕様、61項目の受入検査、P1〜P5の完成条件を変更しない。
