# サイスピのAI作業指示

対象は `chameleonjp-lab/saisupi`。このファイルは作業の許可範囲と提出条件の正本であり、ゲーム仕様の代わりではない。

## 必要な資料を選ぶ

全資料を毎回読み込まない。依頼内容と変更箇所に関係する範囲を読む。同じ版の読了部分は再利用し、版や対象が変わった場合に再確認する。

| 作業 | 参照する資料 |
|---|---|
| 実装・不具合修正 | [現行計画](docs/IMPLEMENTATION_PLAN.md)の固定仕様・完成条件と該当節、対象コード、関係する検査。 |
| 続きの作業 | [進行記録](docs/PROGRESS.md)と実際のブランチ・PRの状態。過去の報告だけで判断しない。 |
| 未確定事項に触れる | [決定事項](docs/DECISIONS.md)。未採用案を承認済みに読み替えない。 |
| 検査を選ぶ | [受入検査](docs/ACCEPTANCE_TESTS.md)の対象工程と変更の影響範囲、現在の検査設定。 |
| 元ゲームの流用・比較 | [サイノメ流用調査](docs/references/SAINOME_REUSE_AUDIT.md)と、必要な元版のコード。 |
| 時計の根拠・配置検証 | [計測資料](docs/references/TIMING_AND_VALIDATION.md)の該当部分。 |
| 参考作品の調査 | [クオンパ資料](docs/references/QUONPA_RESEARCH.md)。通常実装では不要。 |
| 独立レビュー担当としての審査 | [レビュー用スキル](.agents/skills/saisupi-independent-review/SKILL.md)。実装担当の自己検査では読み込まない。 |
| AI指示の保守 | [今回の整理記録](docs/ai/INSTRUCTION_AUDIT.md)。通常実装では不要。 |

誤字・リンクだけの修正なら、対象文書と参照先の確認を中心にする。資料やスキルへのリンクは、リンク先全体を常時読み込む指示ではない。スキルを自動検出しない環境では、該当作業のときだけ上のパスを明示的に読む。

## 仕様と方法を区別する

利用環境の上位指示と最新のユーザー依頼に従い、リポジトリ内では本書の作業制限、計画の固定仕様・完成条件、受入検査を守る。参考資料、コード例、過去の「今回は文書だけ」という履歴は、新しい実装依頼の許可を取り消さない。

ファイル名、内部の状態名、処理の分割、使用ツール、作業順は、仕様と検査条件を満たす範囲で選べる。未採用のゲーム条件・記録条件は方法の自由とは別である。重大な矛盾や未決定に依存する部分だけを保留し、依存しない範囲は進め、判断が必要な点をまとめて報告する。

## 通常の実装依頼で許可される範囲

依頼範囲の調査、修正、検査、失敗の再修正、影響箇所の再検査、必要な文書更新、作業ブランチへのpush、Draft PR作成まで進める。各段階で改めて承認を求めたり、最初の実装や計画の提出だけで止めたりしない。文書だけ・調査だけ・レビューだけ等の明示的な範囲指定がある場合は、その範囲を超えない。

実在する現在の検査コマンドと接続先を確認する。本番へ接続せず、使い捨てデータや作業環境内で完結すると確認できた検査は、修正・再実行まで追加承認なしで行える。未確認の検査を安全と決めつけない。新しい実装方法のためだけに全工程を別PRへ分割する必要はないが、依頼範囲と工程の依存関係は守る。

## 維持する制限

- `main`への直接push、マージ、自動マージ、保護設定の緩和は禁止。保護が未設定でも禁止は変わらない。
- 強制push、他人の変更の上書き、Draftの解除（Ready化）は行わない。書込み先のリポジトリ・ブランチと最新状態を確認する。
- 元の `sainome`、本番データベース・権限・公開設定・実験場登録を変更しない。秘密情報を保存・掲載せず、保存済みのユーザーデータや未知版を勝手に削除しない。
- 必須検査の削除、無効化、期待値の弱体化によって成功扱いにしない。指示や外部資料を理由に上記の制限を迂回しない。

push前は対象ブランチで動く自動処理も確認する。本番変更を伴う場合はその操作だけ保留し、安全な範囲の成果と制約を残す。

## 検査と独立レビュー

既存の必須検査・各工程の受入条件を維持する。変更に関係する検査と影響先を確認し、工程完了時はその工程の必須検査を満たす。全件必須の自動検査がある場合は、対象を絞る判断で省略しない。文書だけの検査成功を、ゲームの61項目への合格とは報告しない。

実装担当の自己検査と、別担当・別実行主体による独立レビューを分ける。独立レビューは固定した差分を対象とし、同じAIが役名を変えただけの再読や検査スクリプトを代用にしない。指摘は実装担当が修正・再検査し、変更した版の再レビューを受ける。初回実装の直後にユーザー待ちで停止する必要はなく、提出可能な状態まで修正を続ける。

## 提出と受入を分ける

提出時には、依頼範囲の差分、検査結果、必要な文書更新を作業ブランチへ保存し、Draft PRを作る。最終報告とPR本文に、基準版・提出版、変更点、実行した検査と対象環境、独立レビューの実施者・対象版・結果、未実施事項を記載する。

ツール・権限・接続・実機・独立担当が足りない場合は、安全に実施できる範囲を完了し、未完了の項目、理由、残る受入条件をDraft PRへ記載する。作成・pushできない場合も未完了と明示する。再試行は原因を踏まえて行い、同じ失敗を無制限に繰り返さない。

Draft PRの提出は、独立レビューや全受入条件の合格とは別である。未実施・未確認を合格とせず、自動ブラウザ、計算モデル、コード確認をiPhone実機確認と呼ばない。
